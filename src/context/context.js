import { createContext } from "react";

// creating context
export const QuizTopicContext = createContext("");

export const AnalysisContext = createContext(0);

export const AuthContext = createContext();
